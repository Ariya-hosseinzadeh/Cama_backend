from django.urls import path

from complaint_ticket import views

urlpatterns = [
    # path('create-ticket/', views.createComplaint.as_view(), name='create_ticket'),
    # path('list-ticket/',views.ComplaintsList.as_view(), name='list-ticket'),
    # path('mycomplaint/',views.Coplaint.as_view(), name='complaint'),
]